To use this program, you need to do the following:
    - Specify your api key in the api_key variable
    - Specify the path to your csv file in path variable
    - Call the get_filtered_df function with the path to your csv file, and the api key as parameters (path, api_key)
    - Call the generate_report function to display the results
        - Check your local project directory for report.png